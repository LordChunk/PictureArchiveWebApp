language: node_js
node_js:
  - "node"
cache: npm
sudo: required
before_script:
  - npm install -g @angular/cli
script:
  - ng build --prod
before_deploy:
  - npm install -g firebase-tools
deploy:
  skip_cleanup: true
  provider: script
  script: firebase deploy --token ${FIREBASE_TOKEN}